# refsOut「它依赖谁」Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引用图出边(refsOut)随 map 落盘 tree.json,viewer 镜像展示「它依赖谁」,「顺便看看」推荐位前置真实依赖(journey.md 与网页同源)。

**Architecture:** centrality.ts 在既有边表上按 from 二次分组(仅 from 为块)产出 refsOut;types/cli 一行式扩展;sequence.ts 的 neighborsOf 变三段拼接去重(refsOut→refsIn 滤非块→章内);serve 层镜像 refsIn 的去 weight+旗标模式;page.ts 泛化 refsHtml 后镜像出第二折叠区与面板段落。学习路径顺序不动。

**Tech Stack:** TypeScript(tsx 直跑,无构建)、vitest、page.ts 内嵌 JS(单引号拼接,**禁反引号与 `\${`**)。

**Spec:** `docs/superpowers/specs/2026-07-15-refsout-design.md`

**基线:** 287 测试全绿,typecheck 干净。完成后 300 测试。

---

## ⚠️ 全计划最高优先级硬约束(每个任务都适用)

`src/grade/centrality.ts` 的**头注释和建边/拆键代码里含反斜杠转义序列的源码文本**(词边界正则、边键分隔符)。本项目已三次发生「转义序列变真实控制字节(0x00-0x08)写进文件」事故。因此:

1. **绝不把含反斜杠转义的行放进 Edit 的 old_string/new_string**——本计划的所有锚点都特意选了不含转义的行,照锚点做。
2. 每次编辑 `src/grade/centrality.ts` 后立刻字节校验:
   `node -e "const b=require('fs').readFileSync('src/grade/centrality.ts');let n=0;for(const x of b)if(x<=8)n++;console.log(n===0?'clean':'CONTROL BYTES '+n);process.exit(n?1:0)"`
3. page.ts 内嵌 JS 禁反引号与 `\${`;所有动态文本过 `esc()`。
4. 本计划所有代码块不含转义序列,照抄即可;自行改写字符串则提交前对改动文件跑上面的字节校验。

---

### Task 1: 数据层——ChunkRefOut / centrality 出边分组 / cli 落盘

**Files:**
- Modify: `src/types.ts`
- Modify: `src/grade/centrality.ts`(锚点编辑,避开转义行)
- Modify: `src/cli.ts`(一行)
- Modify: `test/centrality.test.ts`(追加一个 describe,5 条)
- Modify: `test/cli.test.ts`(既有集成测试加一行断言)

- [ ] **Step 1: 写失败测试**

`test/centrality.test.ts` 第 2 行 import 改为:

```ts
import { referenceGraphCentrality, genericDfCutoff, REFS_IN_TOP_K, REFS_OUT_TOP_K } from '../src/grade/centrality.js';
```

文件末尾追加:

```ts
describe('refsOut(出边镜像,spec:2026-07-15-refsout-design.md)', () => {
  it('小夹具无截断时 refsOut 与 refsIn 边集互为转置(from/to 互换,weight/names 一致)', () => {
    const chunks = [chunk('a.js'), chunk('b.js'), chunk('c.js')];
    const leaves = [leaf('a.js', 'alphaFn'), leaf('b.js', 'betaFn')];
    const sources = {
      'a.js': 'export function alphaFn() {}; betaFn();',
      'b.js': 'export function betaFn() {}; alphaFn();',
      'c.js': 'alphaFn(); betaFn();',
    };
    const { refsIn, refsOut } = referenceGraphCentrality(chunks, leaves, sources);
    const inFlat = Object.entries(refsIn).flatMap(([to, list]) =>
      list.map((r) => ({ from: r.from, to, weight: r.weight, names: r.names })));
    const outFlat = Object.entries(refsOut).flatMap(([from, list]) =>
      list.map((r) => ({ from, to: r.to, weight: r.weight, names: r.names })));
    const key = (e: { from: string; to: string }) => e.from + '->' + e.to;
    expect(outFlat.slice().sort((x, y) => key(x).localeCompare(key(y))))
      .toEqual(inFlat.slice().sort((x, y) => key(x).localeCompare(key(y))));
    expect(outFlat).toHaveLength(4); // a→b, b→a, c→a, c→b
  });

  it('from 非块(sources 多出的文件)不进 refsOut,但仍进 refsIn', () => {
    const chunks = [chunk('lib.js')];
    const leaves = [leaf('lib.js', 'thingFn')];
    const sources = { 'lib.js': 'export function thingFn() {}', 'stray.js': 'thingFn();' };
    const { refsIn, refsOut } = referenceGraphCentrality(chunks, leaves, sources);
    expect(refsIn['lib.js']).toEqual([{ from: 'stray.js', weight: 1, names: ['thingFn'] }]);
    expect(Object.keys(refsOut)).toEqual([]);
  });

  it('出边 top-10 截断:权重降序、平权 to 字典序', () => {
    // hub.js 引用 11 个定义块:d11 定义两个名字(weight 2)排首位,d01..d09 平权字典序,d10 被截
    const files = Array.from({ length: 11 }, (_, i) => 'd' + String(i + 1).padStart(2, '0') + '.js');
    const chunks = [...files.map(chunk), chunk('hub.js')];
    const leaves = files.map((f, i) => leaf(f, 'fn' + String(i + 1).padStart(2, '0')));
    leaves.push(leaf('d11.js', 'extraFn'));
    const sources: Record<string, string> = { 'hub.js': leaves.map((l) => l.name + '();').join(' ') };
    for (const f of files) sources[f] = 'define';
    const { refsOut } = referenceGraphCentrality(chunks, leaves, sources);
    const out = refsOut['hub.js'];
    expect(out).toHaveLength(10);
    expect(out[0]).toEqual({ to: 'd11.js', weight: 2, names: ['extraFn', 'fn11'] });
    expect(out.slice(1).map((r) => r.to)).toEqual(files.slice(0, 9));
    expect(out.map((r) => r.to)).not.toContain('d10.js');
  });

  it('同边多名字聚合:names 字典序', () => {
    const chunks = [chunk('util.js'), chunk('main.js')];
    const leaves = [leaf('util.js', 'zipFn'), leaf('util.js', 'alphaFn')];
    const sources = { 'util.js': 'x', 'main.js': 'zipFn(); alphaFn();' };
    const { refsOut } = referenceGraphCentrality(chunks, leaves, sources);
    expect(refsOut['main.js']).toEqual([{ to: 'util.js', weight: 2, names: ['alphaFn', 'zipFn'] }]);
  });

  it('REFS_OUT_TOP_K 锁 10(UI「前 10」文案依赖此值)', () => {
    expect(REFS_OUT_TOP_K).toBe(10);
  });
});
```

`test/cli.test.ts`:找到既有 runMap 集成测试里的 `expect(tree.refsIn).toBeDefined();` 行,其后加一行:

```ts
    expect(tree.refsOut).toBeDefined();
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/centrality.test.ts test/cli.test.ts`
Expected: 新增 5 条 FAIL(REFS_OUT_TOP_K/refsOut 不存在,可能是编译错),cli 集成测试 FAIL 于 refsOut 断言;原有测试 PASS。

- [ ] **Step 3: types.ts 加 ChunkRefOut 与 Tree.refsOut**

`src/types.ts` 中 `ChunkRefIn` 接口的收尾 `}` 之后(`Tree` 接口之前)插入:

```ts

/** 引用图出边(refsOut;spec:2026-07-15-refsout-design.md) */
export interface ChunkRefOut {
  to: NodeId;        // 被依赖的块 id(恒为块)
  weight: number;    // 与入边同一张边表的权重
  names: string[];   // 命中的名字,字典序
}
```

`Tree` 接口中 `refsIn?: ...` 行之后加一行:

```ts
  refsOut?: Record<NodeId, ChunkRefOut[]>;  // 每块出边 top-10,权重降序;平权 to 字典序
```

- [ ] **Step 4: centrality.ts 四处锚点编辑(全部避开含转义的行)**

**编辑 ①(import)**——第 1 行改为:

```ts
import type { Leaf, Chunk, NodeId, ChunkRefIn, ChunkRefOut } from '../types.js';
```

**编辑 ②(常量与返回类型)**——`export const REFS_IN_TOP_K = 10;` 行之后加一行:

```ts
export const REFS_OUT_TOP_K = 10;
```

`ReferenceGraphResult` 接口的 `refsIn: ...` 行之后加一行:

```ts
  refsOut: Record<NodeId, ChunkRefOut[]>;
```

**编辑 ③(出边收集)**——锚点行 `const inEdges = new Map<string, ChunkRefIn[]>();`(不含转义)之后追加两行:

```ts
  const chunkFiles = new Set(chunks.map((c) => c.file));
  const outEdges = new Map<string, ChunkRefOut[]>();
```

锚点行 `    inEdges.get(to)!.push({ from, weight: w, names: edgeNames.get(k)!.slice().sort() });`(不含转义;它上面那行 split 边键的代码含转义,**绝不要把它包进 old_string**)之后追加:

```ts
    if (chunkFiles.has(from)) { // 出边仅记块(镜像「to 恒为块」;cli 下 sources 与块同源,此为防御)
      if (!outEdges.has(from)) outEdges.set(from, []);
      outEdges.get(from)!.push({ to, weight: w, names: edgeNames.get(k)!.slice().sort() });
    }
```

**编辑 ④(汇总与返回)**——函数尾部这段(不含转义,可整段作 old_string):

```ts
  const refsIn: Record<NodeId, ChunkRefIn[]> = {};
  for (const [to, list] of inEdges) {
    list.sort((a, b) => b.weight - a.weight || (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
    refsIn[to] = list.slice(0, REFS_IN_TOP_K);
  }
  return { centrality, refsIn };
```

替换为:

```ts
  const refsIn: Record<NodeId, ChunkRefIn[]> = {};
  for (const [to, list] of inEdges) {
    list.sort((a, b) => b.weight - a.weight || (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
    refsIn[to] = list.slice(0, REFS_IN_TOP_K);
  }
  const refsOut: Record<NodeId, ChunkRefOut[]> = {};
  for (const [from, list] of outEdges) {
    list.sort((a, b) => b.weight - a.weight || (a.to < b.to ? -1 : a.to > b.to ? 1 : 0));
    refsOut[from] = list.slice(0, REFS_OUT_TOP_K);
  }
  return { centrality, refsIn, refsOut };
```

编辑完立刻跑字节校验(见顶部硬约束第 2 条),必须输出 `clean`。

- [ ] **Step 5: cli.ts 落盘**

`src/cli.ts` 中:

```ts
  const graded = gradeTree({ ...tree, refsIn: ref.refsIn }, {
```

改为:

```ts
  const graded = gradeTree({ ...tree, refsIn: ref.refsIn, refsOut: ref.refsOut }, {
```

- [ ] **Step 6: 跑测试确认通过**

Run: `npx vitest run test/centrality.test.ts test/cli.test.ts`
Expected: 全 PASS(centrality 20 条 = 15 旧 + 5 新)。

- [ ] **Step 7: Commit**

```bash
git add src/types.ts src/grade/centrality.ts src/cli.ts test/centrality.test.ts test/cli.test.ts
git commit -m "feat: 引用图出边 refsOut——同一边表按 from 分组,top-10 落盘 tree.json"
```

---

### Task 2: sequence.ts——「顺便看看」前置真实依赖

**Files:**
- Modify: `src/path/sequence.ts`
- Modify: `test/sequence.test.ts`(追加一个 describe,3 条)

- [ ] **Step 1: 写失败测试**

`test/sequence.test.ts` 文件末尾追加:

```ts
describe('buildPath 邻居:真实依赖前置(spec:2026-07-15-refsout-design.md)', () => {
  const withRefs = (): GradedTree => ({
    ...tree(),
    refsIn: { 'easy.rs': [
      { from: 'hard.rs', weight: 1, names: ['easy'] },
      { from: 'stray.txt', weight: 1, names: ['easy'] },
    ] },
    refsOut: { 'easy.rs': [{ to: 'hard.rs', weight: 2, names: ['core'] }] },
  });

  it('它依赖的块(refsOut to)排最前,跨章也进', () => {
    const path = buildPath(withRefs());
    const step = path.steps.find((s) => s.chunkId === 'easy.rs')!;
    expect(step.neighbors[0]).toBe('hard.rs'); // refsOut,跨章(foo:core)
  });

  it('依赖它的块(refsIn from)次之且滤非块;章内殿后;全程去重不含自己', () => {
    const path = buildPath(withRefs());
    const step = path.steps.find((s) => s.chunkId === 'easy.rs')!;
    expect(step.neighbors).toEqual(['hard.rs', 'mid.rs']); // hard.rs 已由 refsOut 出、refsIn 去重;stray.txt 非块滤掉;mid.rs 章内
    expect(step.neighbors).not.toContain('easy.rs');
    expect(step.neighbors).not.toContain('stray.txt');
  });

  it('无 refsIn/refsOut 的老产物退化为纯章内邻居(既有行为)', () => {
    const path = buildPath(tree());
    const step = path.steps.find((s) => s.chunkId === 'easy.rs')!;
    expect(step.neighbors).toEqual(['mid.rs']);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/sequence.test.ts`
Expected: 前 2 条新测试 FAIL(neighbors 仍是纯章内,`neighbors[0]` 是 'mid.rs' 不是 'hard.rs'),第 3 条与既有 1 条 PASS。

- [ ] **Step 3: 实现 neighborsOf 三段拼接**

`src/path/sequence.ts` 中这段:

```ts
  const neighborsOf = (id: NodeId): NodeId[] => {
    const ch = g.chapters.find((c) => c.id === chunkChapter[id]);
    return ch ? ch.chunkIds.filter((x) => x !== id) : [];
  };
```

替换为:

```ts
  // 邻居三段:真实依赖(refsOut to,权重序)→ 真实被依赖(refsIn from,滤非块)→ 章内其余;按序去重。
  // 学习路径顺序不动,只富化「顺便看看」;老产物(无 refsIn/refsOut)自动退化为纯章内。
  const chunkIds = new Set(g.chunks.map((c) => c.id));
  const neighborsOf = (id: NodeId): NodeId[] => {
    const seen = new Set<NodeId>([id]);
    const out: NodeId[] = [];
    const add = (x: NodeId) => { if (!seen.has(x)) { seen.add(x); out.push(x); } };
    for (const r of g.refsOut?.[id] ?? []) add(r.to);
    for (const r of g.refsIn?.[id] ?? []) if (chunkIds.has(r.from)) add(r.from);
    const ch = g.chapters.find((c) => c.id === chunkChapter[id]);
    for (const x of ch ? ch.chunkIds : []) add(x);
    return out;
  };
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run test/sequence.test.ts`
Expected: 4 条全 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/path/sequence.ts test/sequence.test.ts
git commit -m "feat: 顺便看看前置真实依赖——refsOut/refsIn 邻居三段拼接,journey 与 viewer 同源"
```

---

### Task 3: serve 层——ViewerChunk.refsOut + hasRefsOut

**Files:**
- Modify: `test/viewer-fixture.ts`(末尾追加一个工厂)
- Modify: `test/viewer-state.test.ts`(追加一个 describe,3 条)
- Modify: `src/serve/state.ts`

- [ ] **Step 1: fixture 加双向 refs 树工厂**

`test/viewer-fixture.ts` 第 1 行 import 改为:

```ts
import type { GradedTree, LabelCache, ChunkRefIn, ChunkRefOut } from '../src/types.js';
```

文件末尾追加:

```ts
/** makeViewerTreeWithRefs 之上再加 refsOut:b.rs 依赖 a.rs。 */
export function makeViewerTreeWithRefsOut(): GradedTree {
  const refsOut: Record<string, ChunkRefOut[]> = {
    'crates/foo/src/b.rs': [{ to: 'crates/foo/src/a.rs', weight: 1, names: ['a'] }],
  };
  return { ...makeViewerTreeWithRefs(), refsOut };
}
```

- [ ] **Step 2: 写 3 条失败测试**

`test/viewer-state.test.ts` 第 3 行 import 改为:

```ts
import { makeViewerTree, makeViewerLabels, makeViewerTreeWithRefs, makeViewerTreeWithRefsOut } from './viewer-fixture.js';
```

文件末尾追加:

```ts
describe('buildViewerState refsOut(它依赖谁)', () => {
  it('refsOut 进 ViewerChunk 去 weight,hasRefsOut=true', () => {
    const s = buildViewerState(makeViewerTreeWithRefsOut(), makeViewerLabels(), { version: 1, understood: [] });
    expect(s.hasRefsOut).toBe(true);
    expect(s.chunks[B].refsOut).toEqual([{ to: A, names: ['a'] }]);
    expect(s.chunks[A].refsOut).toEqual([]);
  });

  it('仅 refsIn 的中间期产物 → hasRefsOut=false 且各块 refsOut=[]', () => {
    const s = buildViewerState(makeViewerTreeWithRefs(), makeViewerLabels(), { version: 1, understood: [] });
    expect(s.hasRefs).toBe(true);
    expect(s.hasRefsOut).toBe(false);
    expect(s.chunks[B].refsOut).toEqual([]);
  });

  it('全无的老产物 → 双旗标 false', () => {
    const s = buildViewerState(makeViewerTree(), makeViewerLabels(), { version: 1, understood: [] });
    expect(s.hasRefs).toBe(false);
    expect(s.hasRefsOut).toBe(false);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run test/viewer-state.test.ts`
Expected: 新增 3 条 FAIL,原有 8 条 PASS。

- [ ] **Step 4: 实现 state.ts**

`src/serve/state.ts` 的 `ViewerChunk` 接口中 `refsIn: ...` 行之后加一行:

```ts
  refsOut: { to: NodeId; names: string[] }[]; // 出边(落盘已按权重降序);weight 不出,同 refsIn
```

`ViewerState` 接口中 `hasRefs: ...` 行之后加一行:

```ts
  hasRefsOut: boolean; // tree.refsOut 是否存在;仅 refsIn 的中间期产物此旗标 false
```

`buildViewerState` 中 chunk 字面量的 `refsIn: ...` 行之后加一行:

```ts
      refsOut: (g.refsOut?.[c.id] ?? []).map((r) => ({ to: r.to, names: r.names })),
```

return 对象的 `hasRefs: ...` 行之后加一行:

```ts
    hasRefsOut: g.refsOut !== undefined,
```

- [ ] **Step 5: 跑测试确认通过**

Run: `npx vitest run test/viewer-state.test.ts`
Expected: 11 条全 PASS。

- [ ] **Step 6: Commit**

```bash
git add test/viewer-fixture.ts test/viewer-state.test.ts src/serve/state.ts
git commit -m "feat: serve 层暴露 refsOut(去 weight)与 hasRefsOut 旗标"
```

---

### Task 4: page.ts——refsHtml 泛化 + 第二折叠区 + 面板段落

**Files:**
- Modify: `test/serve-page.test.ts`(追加 2 条 it)
- Modify: `src/serve/page.ts`(七处编辑,全部逐字给出)

- [ ] **Step 1: 写 2 条失败测试**

`test/serve-page.test.ts` 的 describe 末尾追加:

```ts
  it('refsOut: 抽屉第二折叠区与折叠持久化键都在', () => {
    const html = renderPage();
    expect(html).toContain('id="drawer-refs-out"');
    expect(html).toContain('easyreview-refsout-collapsed');
    expect(html).not.toContain('src=');             // 仍自包含
  });

  it('refsOut: 它依赖谁文案与出边空态都在', () => {
    const html = renderPage();
    expect(html).toContain('它依赖谁');
    expect(html).toContain('只统计仓内块之间的引用');
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run test/serve-page.test.ts`
Expected: 新增 2 条 FAIL,原有 5 条 PASS。

- [ ] **Step 3: page.ts 七处编辑**

**编辑 ①(CSS)**——现有三行:

```css
#drawer-refs { padding: 6px 16px; border-bottom: 1px solid var(--border); font-size: 13px; max-height: 30vh; overflow: auto; }
#drawer-refs .refs-head { cursor: pointer; user-select: none; color: var(--accent); font-weight: 600; }
#drawer-refs ul { margin: 6px 0; padding-left: 20px; }
```

改为(选择器各加一个并列项,规则体不动):

```css
#drawer-refs, #drawer-refs-out { padding: 6px 16px; border-bottom: 1px solid var(--border); font-size: 13px; max-height: 30vh; overflow: auto; }
#drawer-refs .refs-head, #drawer-refs-out .refs-head { cursor: pointer; user-select: none; color: var(--accent); font-weight: 600; }
#drawer-refs ul, #drawer-refs-out ul { margin: 6px 0; padding-left: 20px; }
```

**编辑 ②(HTML)**——`<div id="drawer-refs" hidden></div>` 行之后加一行:

```html
  <div id="drawer-refs-out" hidden></div>
```

**编辑 ③(JS 状态变量)**——`var refsCollapsed = ...` 行之后加一行:

```js
var refsOutCollapsed = localStorage.getItem('easyreview-refsout-collapsed') !== 'no'; // 默认折叠
```

**编辑 ④(整段替换依赖关系函数区)**——从 `// ── 被谁依赖(refsIn,中心度 v2 落盘;hasRefs=false 的老产物两处都不渲染) ──` 这行起、到 `renderDrawerRefs` 函数收尾 `}` 止的整段(即 REFS_EMPTY/refTitle/refsHtml/bindRefJumps/renderDrawerRefs 五个定义),替换为:

```js
// ── 依赖关系(refsIn/refsOut,引用图落盘;对应旗标 false 的老产物整段不渲染) ──
var REFS_EMPTY = '未检出(名字级静态扫描,入口文件/动态调用检不到)';
var REFS_OUT_EMPTY = '未检出(名字级静态扫描;只统计仓内块之间的引用)';
function refTitle(n) { return n ? '被谁依赖(' + (n >= 10 ? '前 10' : n) + ')' : '被谁依赖'; }
function refOutTitle(n) { return n ? '它依赖谁(' + (n >= 10 ? '前 10' : n) + ')' : '它依赖谁'; }
function refItems(list, key) { // refsIn 传 'from' / refsOut 传 'to',统一成 {id, names}
  var out = [];
  for (var i = 0; i < list.length; i++) out.push({ id: list[i][key], names: list[i].names });
  return out;
}
function refsHtml(items) { // 通用列表:{id, names}[];id 是块 → 可点跳转(data-ref,避免撞面板既有 .nb[data-id] 绑定),证据名最多 3 个
  var html = '<ul>';
  for (var i = 0; i < items.length; i++) {
    var r = items[i];
    var base = r.id.split('/').pop();
    var src = state.chunks[r.id]
      ? '<span class="nb ref-jump" data-ref="' + esc(r.id) + '" title="' + esc(r.id) + '">' + esc(base) + '</span>'
      : '<span class="muted" title="' + esc(r.id) + '">' + esc(base) + '</span>';
    var evid = r.names.length
      ? ' <span class="muted">(' + esc(r.names.slice(0, 3).join(', ')) + (r.names.length > 3 ? '…' : '') + ')</span>'
      : '';
    html += '<li>' + src + evid + '</li>';
  }
  return html + '</ul>';
}
function bindRefJumps(rootEl) {
  var els = rootEl.querySelectorAll('.ref-jump');
  for (var i = 0; i < els.length; i++) {
    els[i].addEventListener('click', function (ev) {
      selectedId = ev.currentTarget.getAttribute('data-ref');
      openDrawer(selectedId);
      render();
    });
  }
}
function renderDrawerRefs() {
  var box = $('drawer-refs');
  if (!state.hasRefs || !drawerId) { box.hidden = true; return; }
  box.hidden = false;
  var n = state.chunks[drawerId].refsIn.length;
  var html = '<div class="refs-head" id="refs-head">' + (refsCollapsed ? '▸ ' : '▾ ') + refTitle(n) + '</div>';
  if (!refsCollapsed) html += n ? refsHtml(refItems(state.chunks[drawerId].refsIn, 'from')) : '<div class="muted">' + REFS_EMPTY + '</div>';
  box.innerHTML = html;
  $('refs-head').addEventListener('click', function () {
    refsCollapsed = !refsCollapsed;
    localStorage.setItem('easyreview-refs-collapsed', refsCollapsed ? 'yes' : 'no');
    renderDrawerRefs();
  });
  bindRefJumps(box);
}
function renderDrawerRefsOut() {
  var box = $('drawer-refs-out');
  if (!state.hasRefsOut || !drawerId) { box.hidden = true; return; }
  box.hidden = false;
  var n = state.chunks[drawerId].refsOut.length;
  var html = '<div class="refs-head" id="refs-out-head">' + (refsOutCollapsed ? '▸ ' : '▾ ') + refOutTitle(n) + '</div>';
  if (!refsOutCollapsed) html += n ? refsHtml(refItems(state.chunks[drawerId].refsOut, 'to')) : '<div class="muted">' + REFS_OUT_EMPTY + '</div>';
  box.innerHTML = html;
  $('refs-out-head').addEventListener('click', function () {
    refsOutCollapsed = !refsOutCollapsed;
    localStorage.setItem('easyreview-refsout-collapsed', refsOutCollapsed ? 'yes' : 'no');
    renderDrawerRefsOut();
  });
  bindRefJumps(box);
}
```

(与现状的差异:REFS_OUT_EMPTY/refOutTitle/refItems/renderDrawerRefsOut 为新增;refsHtml 从"读 refsIn 的 id"泛化为"接收 {id, names}[]";renderDrawerRefs 的两处调用改经 refItems;其余逐字保留。)

**编辑 ⑤(openDrawer)**——`  renderDrawerRefs();` 行之后加一行 `  renderDrawerRefsOut();`。

**编辑 ⑥(closeDrawer)**——`  $('drawer-refs').hidden = true;` 行之后加一行:

```js
  $('drawer-refs-out').hidden = true;
```

**编辑 ⑦(render 与 renderPanel)**——render() 中:

```js
  if (drawerId) { renderDrawerHead(); renderDrawerFns(); renderDrawerRefs(); } // done 后按钮/✓ 即时更新
```

改为:

```js
  if (drawerId) { renderDrawerHead(); renderDrawerFns(); renderDrawerRefs(); renderDrawerRefsOut(); } // done 后按钮/✓ 即时更新
```

renderPanel 中现有段:

```js
  if (state.hasRefs) {
    html += c.refsIn.length
      ? '<p><b>' + refTitle(c.refsIn.length) + '</b></p>' + refsHtml(showId)
      : '<p class="muted"><b>被谁依赖:</b>' + REFS_EMPTY + '</p>';
  }
```

改为(refsHtml 改经 refItems,并加「它依赖谁」段):

```js
  if (state.hasRefs) {
    html += c.refsIn.length
      ? '<p><b>' + refTitle(c.refsIn.length) + '</b></p>' + refsHtml(refItems(c.refsIn, 'from'))
      : '<p class="muted"><b>被谁依赖:</b>' + REFS_EMPTY + '</p>';
  }
  if (state.hasRefsOut) {
    html += c.refsOut.length
      ? '<p><b>' + refOutTitle(c.refsOut.length) + '</b></p>' + refsHtml(refItems(c.refsOut, 'to'))
      : '<p class="muted"><b>它依赖谁:</b>' + REFS_OUT_EMPTY + '</p>';
  }
```

(面板既有的 `bindRefJumps($('panel'));` 一次绑定即覆盖两个列表,不用动。)

- [ ] **Step 4: 跑 page 测试确认通过**

Run: `npx vitest run test/serve-page.test.ts`
Expected: 7 条全 PASS。

- [ ] **Step 5: 全量回归 + typecheck**

Run: `npm test`
Expected: 62 文件 300 测试全 PASS。
Run: `npm run typecheck`
Expected: 干净无错误。

- [ ] **Step 6: Commit**

```bash
git add test/serve-page.test.ts src/serve/page.ts
git commit -m "feat: 它依赖谁面板——refsHtml 泛化,第二折叠区+面板段落镜像,双旗标零回归"
```
